schemapack: 0.1.0
classes:
  Dataset:
    id:
      from: alias
    content: ../content_schemas/Dataset.schema.json
    relations:
      files:
        class: DoesNotExist # <-
        cardinality: many-to-many
        lookup: in-document
