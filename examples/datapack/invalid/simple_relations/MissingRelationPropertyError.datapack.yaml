# Misses relations property defined in the schemapack:
datapack: 0.2.0
resources:
  File: {}
  Dataset:
    example_dataset:
      content:
        dac_contact: dac@example.org
      relations: {} # should contain `files` property
