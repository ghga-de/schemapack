# One relation points to a non-existing resource:
datapack: 0.2.0
resources:
  File: {}
  Dataset:
    example_dataset:
      content:
        dac_contact: dac@example.org
      relations:
        files:
          - not_existing_file # <-
