# The corresponding schemapack expects no root but a root resource is defined:
datapack: 0.3.0
resources:
  SomeClass:
    a:
      content: {}
      relations:
        some_relation:
          - b
    b:
      content: {}
      relations:
        some_relation:
          - a
rootResource: a # <-
