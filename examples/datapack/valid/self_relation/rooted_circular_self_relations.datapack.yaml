# This datapack contains a file that is in relation to itself:
# (This is valid, but no integration can be performed.)
datapack: 0.1.0
resources:
  SomeClass:
    a:
      content:
        alias: a
      relations:
        some_relation:
          - a # <-
root:
  class_name: SomeClass
  resource_id: a
