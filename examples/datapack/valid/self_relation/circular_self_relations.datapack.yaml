# This datapack contains a file that is in relation to itself:
# (This is valid, but no denomalization can be performed.)
datapack: 0.2.0
resources:
  SomeClass:
    a:
      content: {}
      relations:
        some_relation:
          - a # <-
