description: Refering to a non-existing class as root
schemapack: 0.2.0
classes:
  SomeClass:
    id:
      propertyName: alias
    content: ../../content_schemas/AnyObject.schema.json
    relations:
      some_relation:
        targetClass: SomeClass
        mandatory:
          origin: true
          target: true
        multiple:
          origin: true
          target: true
        lookup: in_document
root_class: NonExistingClass # <-
